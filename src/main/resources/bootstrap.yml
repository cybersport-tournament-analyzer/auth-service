spring:
  application:
    name:
      user-service
  cloud:
    config:
      enabled: true
      fail-fast: true
      label: remote-config
  config:
    import: optional:config-service:http://5.34.209.144:8888/
  profiles:
    active: dev

eureka:
  client:
    service-url:
      uris: http://5.34.209.144:8761/eureka/
      defaultZone: http://5.34.209.144:8761/eureka/
    healthcheck:
      enabled: true
    enabled: true
  instance:
    secure-port-enabled: true
    non-secure-port-enabled: false
    instance-id: ${spring.application.name}